class TransactionItem {
    
    constructor(productId, product, price, quantity, subtotal) {
        this.productId = productId;
        this.product = product
        this.price = price
        this.quantity = quantity
        this.subtotal = subtotal
    }
    
}